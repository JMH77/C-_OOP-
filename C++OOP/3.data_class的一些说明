1.注意类内成员函数的传参与否
    大部分未传参函数使用的是成员变量，就是private下面的，如本程序的  isLeapYear（）
    但是也有像这个dateToDays()这种，前面的成员函数不仅涉及自身这个对象的转换，可能还需要与其他日期比较，那就需要进行一个参数传递。

2.日期计算的小技巧

3.<ctime>
    包括 time() 、 *tm、time_t这些，有的是函数有的是类型，需要区分清楚，多查资料
